// Thin wrapper to reuse the root Vite config used by Lovable
// This file exists because build scripts reference --config config/vite.config.ts
import config from '../vite.config';
export default config;
